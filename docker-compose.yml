version: '2'

volumes:
  network_events_data:

services:
  monitor:
    build: ./monitor
    volumes:
      - network_events_data:/app/data
    environment:
      - 'DBUS_SYSTEM_BUS_ADDRESS=unix:path=/host/run/dbus/system_bus_socket'
    labels:
      io.balena.features.dbus: '1'